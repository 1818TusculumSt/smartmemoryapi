# SmartMemory API - GPU Requirements (Full CUDA build ~12GB)
# Use this for GPU-accelerated local embeddings
# Requires: NVIDIA GPU with CUDA support

# FastAPI and ASGI server
fastapi==0.115.4
uvicorn[standard]==0.32.0

# Pydantic for validation
pydantic==2.9.2
pydantic-settings==2.6.1

# Vector database
pinecone-client==5.0.1

# Embeddings - Full PyTorch with CUDA support (includes 3.8GB CUDA libraries)
sentence-transformers==3.3.1
numpy==2.0.2

# Async HTTP client with HTTP/2
httpx[http2]==0.27.2

# Environment variables
python-dotenv==1.0.1
